@model IEnumerable<MovieWeb.Models.MovieUrls_64130299>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Url)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie_64130299.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Url)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie_64130299.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MovieUrlId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MovieUrlId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MovieUrlId })
        </td>
    </tr>
}

</table>
